<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>ToDo - Shantanu Patil | TaskToDo</title>
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <!-- Popper JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>
    <link href="/styles/main.css" rel="stylesheet"/>
  </head>
  <body>
    <div class="container" style="margin-top: 50px;">
      <h1>Hello, You have <%= count %> task left</h1>
      <form action="/add" method="POST" style="margin-top: 50px;">
        <div class="form-group">
          <label for="description">Enter task</label>
          <input class="form-control block" type="text" name="description" id="description" placeholder="Task Description">
        </div>
        <button class="btn btn-primary" type="submit" name="button">Add task</button>
      </form>

      <div class="row" style="margin-top: 50px;">
        <div class="col-md-6">
          <h1>Your Dues</h1>
          <ul class="list-group">
            <% for(let i = 0; i < task.length; i++) { %>
              <% if(!task[i].done) { %>
                <form action='/task/<%= task[i]._id %>/completed' method="post">
                  <li class="list-group-item"><%= task[i].description %> <button class="btn-complete" type="submit">✓</button></li>
                </form>
              <% } %>
            <% } %>
          </ul>
        </div>

        <div class="col-md-6">
          <h1>Completed Task</h1>
          <ul class="list-group">
            <% for(let i = 0; i < task.length; i++) { %>
              <% if(task[i].done) { %>
                <form action='/task/<%= task[i]._id %>/notcompleted' method="post">
                  <li class="list-group-item"><%= task[i].description %> <button class="btn-complete" type="submit">X</button></li>
                </form>
              <% } %>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
  </body>
</html>
